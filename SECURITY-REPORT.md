# تقرير المراجعة الأمنية الشامل
**المشروع:** منصة الخطة التشغيلية المدرسية  
**التاريخ:** 2026-08-06  
**نطاق المراجعة:** جميع ملفات المشروع (`index.html`, `script.js`, `config.js`, `style.css`, `README.md`) + طبقة Supabase

---

## ملخص تنفيذي

تمت مراجعة أمنية كاملة للمشروع وإصلاح الثغرات القابلة للإصلاح في الواجهة دون كسر الوظائف الحالية.  
تم إنشاء ملف `supabase-security.sql` يجب تنفيذه يدوياً في لوحة Supabase لإغلاق أخطر ثغرة على مستوى قاعدة البيانات (جدول `users` + سياسة `allow_all`).

| المستوى | العدد |
|--------|------|
| حرج (Critical) | 4 |
| عالي (High) | 5 |
| متوسط (Medium) | 6 |
| منخفض (Low) | 3 |

---

## 1) الثغرات المكتشفة وما تم إصلاحه

### CRITICAL

| # | الثغرة | الخطورة | الحالة | التفاصيل |
|---|--------|---------|--------|----------|
| C1 | سياسة RLS `allow_all` على كل الجداول | Critical | **يحتاج تدخل يدوي** | كانت تسمح لأي شخص يملك anon key بقراءة/تعديل/حذف كل شيء بما فيها كلمات المرور. ملف `supabase-security.sql` يغلق جدول `users` ويستبدل السياسات. |
| C2 | كلمات مرور بـ plaintext في جدول `users` مع إمكانية `SELECT *` | Critical | **جزئي — يدوي مطلوب** | الواجهة لم تعد تطلب عمود password في الاستعلامات. دوال `authenticate_user` / `admin_*` تمنع الوصول المباشر بعد تنفيذ SQL. |
| C3 | تصعيد صلاحيات: أي زائر يستطيع إدراج مستخدم بدور `admin` عبر API | Critical | **جزئي — يدوي مطلوب** | بعد تنفيذ SQL يصبح الإنشاء عبر `admin_add_user` فقط (يتحقق من أن المستدعي admin). الواجهة محدّثة لاستخدام RPC. |
| C4 | تحميل كل بيانات المنصة بدون تسجيل دخول عند `DOMContentLoaded` | Critical | **أُصلح** | لا تُحمَّل البيانات إلا بعد جلسة صالحة. |

### HIGH

| # | الثغرة | الخطورة | الحالة | التفاصيل |
|---|--------|---------|--------|----------|
| H1 | XSS عبر `innerHTML` بدون تهريب (أسماء برامج، ملاحظات، روابط…) | High | **أُصلح** | إضافة `escapeHtml` / `esc` على كل المخرجات الديناميكية. |
| H2 | روابط `javascript:` / `data:` (XSS عبر الروابط) | High | **أُصلح** | `sanitizeUrl` + `safeLinkHtml` تسمح بـ http/https فقط. |
| H3 | تخزين `currentUser` في `localStorage` (قابل للسرقة عبر XSS) | High | **أُصلح** | الجلسة في `sessionStorage` بمُعرّف وبريد فقط، وتُعاد المصادقة من الخادم عند الاستعادة. |
| H4 | تجاوز صفحات عبر استدعاء `navTo('users')` من الكونسول | High | **أُصلح** | فحص `isSectionAllowed` قبل التنقل. |
| H5 | إدارة مستخدمين بدون تحقق صلاحيات في الدوال | High | **أُصلح** | `requireAuth('manageUsers')` على الإضافة/الحذف/تغيير الدور + منع تغيير/حذف الحساب الحالي. |

### MEDIUM

| # | الثغرة | الخطورة | الحالة | التفاصيل |
|---|--------|---------|--------|----------|
| M1 | رفع ملفات بدون تحقق صارم من الامتداد | Medium | **أُصلح** | قائمة بيضاء للامتدادات + حد 5MB + تحقق MIME للصور. |
| M2 | غياب تحقق طول المدخلات | Medium | **أُصلح** | `clampInput` بحد أقصى 500/1000 حرف. |
| M3 | كلمات مرور تجريبية ضعيفة `1234` | Medium | **أُصلح في الكود** | غُيّرت إلى `ChangeMe!1234` — يجب تغييرها في قاعدة البيانات يدوياً إن وُجدت الحسابات القديمة. |
| M4 | غياب CSP وheaders أمنية | Medium | **أُصلح جزئياً** | أُضيفت CSP meta + nosniff في HTML. الـ headers على مستوى الاستضافة تحتاج ضبط يدوي. |
| M5 | تعديل حالة المهمة بدون صلاحية | Medium | **أُصلح** | `chgTaskStatus` يتطلب `editTask`. |
| M6 | `handleDelInd` غير موجود (زر حذف معطل/خطأ) | Medium | **أُصلح** | أُنشئت الدالة مع فحص صلاحية. |

### LOW

| # | الثغرة | الخطورة | الحالة | التفاصيل |
|---|--------|---------|--------|----------|
| L1 | CDN بدون تثبيت إصدار دقيق | Low | **أُصلح** | تثبيت `@supabase/supabase-js@2.49.1`. |
| L2 | HTML مكسور في `ev-program-select` | Low | **أُصلح** | إزالة النص المكرر. |
| L3 | بيانات كاش في localStorage (غير حساسة لكن قابلة للتلاعب محلياً) | Low | مقبول | كاش عرض فقط؛ المصدر الحقيقي هو Supabase بعد تسجيل الدخول. |

---

## 2) ما تم فحصه وتأكدت سلامته نسبياً

- **لا يوجد service_role key** في المشروع — anon key فقط في `config.js` (هذا صحيح للواجهة).
- **SQL Injection:** الاستعلامات عبر Supabase client (مُعلَّمة) — لا يوجد بناء SQL يدوي من المدخلات.
- **صلاحيات الواجهة:** مصفوفة `PERMS` موجودة لكل دور، وأُضيفت فحوصات على العمليات الحساسة والتنقل.
- **إخفاء كلمات المرور من الجلسة:** لا تُخزَّن كلمة المرور في sessionStorage.

---

## 3) ما يحتاج تدخلاً يدوياً (مهم جداً)

### أ) تنفيذ ملف الأمان على Supabase (إلزامي)

1. افتح **Supabase Dashboard → SQL Editor**.
2. نفّذ محتوى الملف: `supabase-security.sql`.
3. غيّر كلمات مرور الحسابات التجريبية:

```sql
UPDATE users SET password = 'كلمة_قوية_خاصة_بك' WHERE email = 'admin@school.sa';
UPDATE users SET password = 'كلمة_قوية_خاصة_بك' WHERE email = 'vice@school.sa';
UPDATE users SET password = 'كلمة_قوية_خاصة_بك' WHERE email = 'teacher@school.sa';
```

### ب) مخاطر متبقية بسبب طبيعة المعمارية (Frontend-only)

المشروع تطبيق واجهة أمامية فقط يعتمد مصادقة مخصصة (جدول `users` + مقارنة كلمة المرور) وليس **Supabase Auth**.  
لذلك:

| المخاطر المتبقية | الخطورة | الحل الموصى به |
|------------------|---------|----------------|
| سياسات الكتابة على جداول البيانات (`programs`, `tasks`…) ما زالت مفتوحة للـ anon بعد تنفيذ SQL الحالي | High | الانتقال إلى Supabase Auth + سياسات RLS مبنية على `auth.uid()` والأدوار |
| كلمات المرور مخزّنة plaintext (ليست bcrypt) | High | استخدام `auth.users` أو تخزين `crypt()` / bcrypt عبر دالة SECURITY DEFINER |
| anon key ظاهر في الواجهة | مقبول | anon key عام بطبعه؛ الحماية الحقيقية من RLS فقط |
| لا يوجد Rate limiting على تسجيل الدخول | Medium | تفعيل حماية على مستوى Edge Function / Cloudflare / Supabase Auth |

### ج) إعدادات استضافة موصى بها

أضف headers على Netlify/Vercel/Nginx:

```
Content-Security-Policy: (نفس سياسة الـ meta أو أشد)
X-Frame-Options: DENY
X-Content-Type-Options: nosniff
Referrer-Policy: strict-origin-when-cross-origin
Permissions-Policy: camera=(), microphone=(), geolocation=()
```

### د) تدوير مفتاح anon (اختياري لكن مُستحسن)

بما أن المفتاح كان في المستودع، يُفضَّل من لوحة Supabase إعادة توليد المفاتيح إن كان المشروع عاماً على GitHub، ثم تحديث `config.js`.

---

## 4) الملفات التي تغيّرت

| الملف | التغيير |
|-------|---------|
| `script.js` | طبقة أمان، جلسات، XSS، تحقق صلاحيات، رفع ملفات، RPC للمستخدمين |
| `index.html` | CSP، إصلاح HTML، قبول امتدادات صور آمنة، تثبيت إصدار المكتبة |
| `supabase-security.sql` | **جديد** — دوال مصادقة + إغلاق جدول users |
| `README.md` | تحديث كلمات المرور التجريبية وتعليمات الأمان |
| `SECURITY-REPORT.md` | **جديد** — هذا التقرير |

---

## 5) المراجعة الأمنية الثانية (بعد الإصلاحات)

بعد تطبيق الإصلاحات أُجري فحص ثانٍ:

| الفحص | النتيجة |
|-------|---------|
| وجود `service_role` في الكود | غير موجود |
| تخزين كلمة مرور في localStorage/sessionStorage | غير موجود |
| تحميل بيانات بدون جلسة | محظور |
| XSS عبر حقول النص المعروضة | مُهرَّبة بـ `esc()` |
| روابط خبيثة | مرفوضة عبر `sanitizeUrl` |
| تجاوز التنقل للصفحات | محظور عبر `isSectionAllowed` |
| عمليات المستخدمين بدون صلاحية | محظورة في الواجهة + RPC بعد تنفيذ SQL |
| رفع ملفات تنفيذية (.exe/.html/.svg) | مرفوض بالقائمة البيضاء |
| خطأ `supabase` غير المعرّف في `updateProgramProgress` | أُصلح إلى `sb` |
| زر حذف مؤشر بدون دالة | أُصلحت بإضافة `handleDelInd` |

**الخلاصة بعد المراجعة الثانية:**  
الواجهة أكثر أماناً بشكل ملحوظ، لكن **لا يُعتبر المشروع آمناً للإنتاج** حتى يتم تنفيذ `supabase-security.sql` والانتقال لاحقاً إلى Supabase Auth + تشفير كلمات المرور.

---

## 6) خطة تحسين موصى بها (مرحلة لاحقة)

1. تفعيل **Supabase Auth** (email/password) وإلغاء جدول كلمات المرور المخصص.
2. سياسات RLS حسب الدور عبر claim في JWT أو جدول `profiles`.
3. تخزين الملفات في **Supabase Storage** بدل base64 في قاعدة البيانات.
4. تفعيل **CAPTCHA / rate limit** على تسجيل الدخول.
5. مراجعة دورية للتوابع عبر `npm audit` إن انتقل المشروع لـ bundler.

---

*إعداد: مراجعة أمنية Full Stack — آب 2026*
