# قائمة تحقق أمنية — security-hardening

استخدم هذه القائمة قبل أي نشر لفرع الحماية. لا تنفّذ بنود قاعدة البيانات تلقائياً من الواجهة.

## 1) بيانات الدخول المكشوفة

- [ ] إزالة كل كلمات المرور وأسماء المستخدمين الحقيقية من Git
- [ ] تدوير كلمات المرور التي ظهرت سابقاً في README / الكود / المحادثات (يدوياً في Supabase)
- [ ] التأكد أن المستودع لا يحتوي `FALLBACK_USERS` أو seed بكلمات مرور
- [ ] عدم إعادة إدخال بيانات دخول حقيقية في الملفات

## 2) تفعيل Supabase Auth

- [ ] تفعيل مزوّد Email في Authentication
- [ ] إنشاء حسابات Auth للأدوار الثلاثة على بيئة تجريبية
- [ ] ربط الدور (`admin` / `vice` / `teacher`) بملف المستخدم
- [ ] اتباع `SECURITY-MIGRATION-PLAN.md` قبل قطع الدخول القديم

## 3) فحص RLS لكل جدول

- [ ] `users` / `profiles`
- [ ] `programs`
- [ ] `program_indicators`
- [ ] `initiatives`
- [ ] `tasks`
- [ ] `evidences`
- [ ] `teacher_followups`
- [ ] `settings`
- [ ] `school_years`
- [ ] التأكد أن `anon` لا يملك كتابة عامة بعد الهجرة

## 4) تحويل Storage إلى Private

- [ ] اختبار محلي لـ `createSignedUrl` على فرع `security-hardening`
- [ ] تشغيل يدوي لاحق لـ `sql/security_storage_private.sql`
- [ ] التحقق أن `evidences.public = false`
- [ ] التحقق أن سياسات anon المفتوحة حُذفت
- [ ] التحقق أن الملفات القديمة ما زالت موجودة ويمكن فتحها عبر signed URL

## 5) اختبار الأدوار

- [ ] admin: دخول + إدارة مستخدمين + رفع شاهد + عرض ملف
- [ ] vice: دخول + صلاحيات الوكيل المتوقعة فقط
- [ ] teacher: دخول + رفع شاهد دون صلاحيات الإدارة

## 6) اختبار منع المستخدم غير المسجّل

- [ ] بدون جلسة: لا تحميل بيانات تشغيلية حساسة
- [ ] بدون جلسة: فشل رفع ملف إلى Storage
- [ ] بدون جلسة: فشل إنشاء signed URL / فتح ملفات خاصة

## 7) النسخ الاحتياطي

- [ ] نسخة احتياطية لقاعدة البيانات (Dashboard → Backups / dump)
- [ ] نسخة/جرد لملفات bucket `evidences`
- [ ] حفظ نقطة استعادة قبل تشغيل SQL الخاص بالـ Storage أو RLS

## 8) خطة Rollback

1. أبقِ فرع `before-school-year-system` كما هو دون تعديل مباشر أثناء التجربة.
2. إن فشل اختبار Auth/Storage على `security-hardening`: لا تدمج إلى الفرع المنشور.
3. إن شُغّل `security_storage_private.sql` وظهر عطل في العرض:
   - أعد مؤقتاً `public = true` للـ bucket من SQL Editor (يدوياً) **فقط** كإجراء طوارئ قصير
   - أو أعد السياسات السابقة من نسخة احتياطية
4. لا تحذف ملفات Storage كجزء من Rollback.
5. وثّق سبب التراجع والخطوة التالية قبل أي محاولة نشر جديدة.

## حالة هذه المرحلة

- فرع العمل: `security-hardening`
- لم يُنفَّذ SQL تلقائياً
- لم يُعمل Commit/Push في هذه الخطوة إلا إذا طلب المستخدم لاحقاً
